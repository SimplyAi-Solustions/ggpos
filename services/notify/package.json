{
  "name": "@gg/notify",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Push sidecar: every minute, sends Web Push for notifications rows PocketBase has not pushed yet",
  "main": "./src/index.mjs",
  "scripts": {
    "start": "node ./src/index.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
